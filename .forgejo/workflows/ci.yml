name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Set up virtualenv
        run: |
          python3 -m venv .venv
          echo "$PWD/.venv/bin" >> $GITHUB_PATH

      - name: Install dependencies
        run: |
          . .venv/bin/activate
          pip install -r requirements.txt
          pip install pytest

      - name: Run tests
        run: |
          . .venv/bin/activate
          python -m pytest tests/test_production_ready.py tests/test_api_routes.py tests/test_eula.py tests/test_scanners.py -v --tb=short

      - name: Verify compilation
        run: |
          . .venv/bin/activate
          python -c "
          import py_compile, glob, sys
          files = glob.glob('lib/*.py') + glob.glob('scanners/*.py') + glob.glob('web/*.py')
          errors = []
          for f in files:
              try:
                  py_compile.compile(f, doraise=True)
              except py_compile.PyCompileError as e:
                  errors.append(str(e))
          if errors:
              for e in errors:
                  print(f'COMPILE ERROR: {e}', file=sys.stderr)
              sys.exit(1)
          print(f'Successfully compiled {len(files)} files')
          "
